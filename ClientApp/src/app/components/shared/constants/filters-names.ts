import { FilterName } from 'src/app/shared/enums/filter-name.enum';

export const FILTERS_NAMES = {
  ArrivalYear: FilterName.holidayYear,
  KeyPeriod: FilterName.keyPeriodName,
  ArrivalWeek: FilterName.weekNumber,
  RmResponsible: FilterName.responsibleRevenueManager,
  Region: FilterName.regionName,
  Park: FilterName.parkName,
  AccommodationType: FilterName.accommTypeName,
  Bedrooms: FilterName.accommBeds,
  sGradeTier: FilterName.unitGradeName,
  Service: FilterName.accommName
};
